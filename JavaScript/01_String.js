const gameName = 'bhavin-malaviya';

const matchword = gameName.match('bhav');
console.log(matchword);

const lengthWord = gameName.length;
console.log(lengthWord);

const indexOfWord = gameName.indexOf('a')
console.log(indexOfWord);

const charAtWord = gameName.charAt(4);
console.log(charAtWord);

// replace
// repeat
// search
// split
// slice  
// substring
// sup
// toLowerCase
// toUpperCase
// trim
// toFixed
// toPrecision
// toLocaleString

//    # Math

// abs
// round
// ceil
// floor
// sqrt


const min = 10
const max = 20

console.log(Math.floor(Math.random() * (max - min + 1)) + min);

// Prefix And Postfix Oparations 